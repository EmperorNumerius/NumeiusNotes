--- lib/widgets/tab_manager.dart
+++ lib/widgets/tab_manager.dart
@@ -93,12 +93,13 @@
                           const SizedBox(width: 4),
                           // Close button
                           if (tabs.length > 1)
-                            GestureDetector(
-                              onTap: () => docMgr.closeTab(i),
-                              child: Padding(
-                                padding: const EdgeInsets.all(2),
-                                child: Icon(
+                            IconButton(
+                              onPressed: () => docMgr.closeTab(i),
+                              padding: const EdgeInsets.all(2),
+                              constraints: const BoxConstraints(),
+                              tooltip: 'Close tab',
+                              icon: Icon(
                                   Icons.close,
                                   size: 12,
                                   color: Colors.white.withAlpha(60),
                                 ),
-                              ),
                             ),
                         ],
