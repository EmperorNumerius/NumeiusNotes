## 2026-06-26 - Confirmation Dialog on Destructive Actions\n**Learning:** Destructive actions like deleting a note or folder need an explicit confirmation step (like a modal dialog) to prevent accidental data loss and improve user safety.\n**Action:** Add a `showDialog` confirmation helper (e.g., `_confirmDelete`) for all destructive operations before executing the underlying logic.
