## 2025-02-14 - Add tooltips to IconButtons
**Learning:** IconButtons used without tooltip arguments make the interface less accessible to screen reader users and users navigating by mouse hover who don't know what an icon implies.
**Action:** Always add `tooltip` text describing the action on an `IconButton` so it is read aloud and shown on hover.