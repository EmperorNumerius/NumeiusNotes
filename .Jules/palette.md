## 2024-09-21 - Added Semantic HTML attributes
**Learning:** Found instances of gesture detectors without keyboard operability and buttons lacking explicit semantic states/AROA labels.
**Action:** When finding UI elements like `Icon` wrapped in `GestureDetector`, upgrade them to `IconButton` with `tooltip` wherever applicable.
