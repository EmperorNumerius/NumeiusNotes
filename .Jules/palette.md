## 2026-08-01 - Replaced GestureDetector with IconButton
**Learning:** Replaced GestureDetector with IconButton for icon-only buttons to automatically add ARIA/tooltip support and ensure keyboard focus states while preserving layout with zero padding and BoxConstraints().
**Action:** Use IconButton with tooltips instead of GestureDetector for icon-only buttons in Flutter.
