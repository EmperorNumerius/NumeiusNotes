// Placeholder test — the full app requires async init and providers,
// so integration testing is recommended over widget testing.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Placeholder test', () {
    expect(1 + 1, 2);
  });
}
